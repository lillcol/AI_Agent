# Public configuration (safe to commit)
app:
  name: ai-agent-learning
  env: dev

logging:
  level: INFO

services:
  minimax:
    base_url: "https://api.minimax.chat/v1"
  deepseek:
    base_url: "https://api.deepseek.com"
  amap_weather:
    base_url: "https://restapi.amap.com/v3/weather"
