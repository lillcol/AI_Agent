# Private configuration template (DO NOT put real secrets here)
# Copy to one of the following local files:
# - configs/private.local.yaml
# - configs/private.yaml
#
# Both are ignored by .gitignore.

services:
  minimax:
    api_key: ""
  deepseek:
    api_key: ""
  amap_weather:
    api_key: ""
